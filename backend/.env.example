JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
PORT=3000
DATABASE_TYPE=sqlite
SQLITE_PATH=./data/study.sqlite

# PostgreSQL (when DATABASE_TYPE=postgres)
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=study
# DATABASE_PASSWORD=study_secret
# DATABASE_NAME=study_system
